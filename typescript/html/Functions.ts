// function that provides locationId for the event
let locationIdFunction = (event: IEvent) => event.locationId;
let audienceIdFunction = (event: IEvent) => event.audienceId;
let trackIdFunction = (event: IEvent) => event.trackId;
